apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
  - host: "bankapp.internal.com"
    http:
      paths:
        - pathType: Prefix
          path: "/"
          backend:
            service:
              name: front
              port:
                number: 8080